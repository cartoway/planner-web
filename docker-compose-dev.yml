services:
  db:
    image: postgres:9.6 # Required version to run tests with Rails 4
    volumes:
      - ./app:/srv/app/app

  web:
    volumes:
      - .:/srv/app
    ports:
      - 127.0.0.1:8180:8080

  delayed-job:
    volumes:
      - .:/srv/app
    deploy:
      replicas: 1

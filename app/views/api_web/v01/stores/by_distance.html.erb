<div id="map"></div>
<%
controller.js(
  map_layer_url: request.ssl? ? current_user.layer.urlssl : current_user.layer.url,
  map_lat: @position.lat,
  map_lng: @position.lng,
  map_attribution: current_user.layer.map_attribution,
  ids: @stores.collect(&:id),
  display_home: true
)
%>

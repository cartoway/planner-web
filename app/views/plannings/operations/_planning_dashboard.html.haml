- if @planning.customer.reseller.planning_dashboard_url && @manage_planning[:manage_planning_dashboard]
  .col
    = link_to @planning.customer.reseller.planning_dashboard_url.gsub('{LG}', I18n.locale.to_s).gsub('{ID}', current_user.customer.id.to_s).gsub('{P_ID}', @planning.id.to_s), target: '_blank', class: 'btn btn-default', title: t('analytics.index.link'), disabled: @manage_planning[:disable_planning_dashboard] do
      %i.fa.fa-chart-line.fa-fw

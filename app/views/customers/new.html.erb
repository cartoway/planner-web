<% javascript 'customer' %>

<% content_for :title, t('.title') %>

<h1><%= t '.title' %></h1>

<%= render 'form' %>

<%
  routers = Router.all

  controller.js(
    admin: true,
    routers_options: Hash[routers.collect { |r| [r.id, r.options] }],
    routers_modes_by_profile: raw(routers_modes_by_profile.to_json),
    default_profile_router: reseller_default_profile_router_combined_value(current_user.reseller)
  )
%>

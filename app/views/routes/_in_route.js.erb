$('ol.routes').find("li.route[data-route-id='<%= @route.id %>']")
  .attr('data-size', <%= route[:size].to_i %>)
  .removeData('size')
  .html("<%= j(render 'routes/in_route.html.haml', local_assigns) %>");
var locals = <%= local_assigns.to_json.html_safe %>;

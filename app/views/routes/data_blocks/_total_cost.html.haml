- ui_order = local_assigns.fetch(:ui_order, 0)
- if route[:vehicle_id] && route[:route_averages] && route[:total_cost]
  .col-xs-4{title: t('plannings.edit.cost_help'), style: "order: #{ui_order}"}
    .primary.route-info
      %i.fa.fa-coins.fa-fw
      = route[:total_cost]
      = t("all.unit.currency_symbol.#{route[:route_averages][:prefered_currency]}")

- routes.each do |route|
  - if route[:vehicle_id]
    %li.route{"data-color": route[:color], "data-default-color": route[:default_color], "data-route-id": route[:route_id], "data-size": route[:size]}
      = render partial: 'routes/in_route.html.haml', locals: { route: route, summary: summary }

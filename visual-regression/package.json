{
  "name": "planner-web-visual-regression",
  "private": true,
  "description": "Playwright visual snapshots for Lookbook previews (run from repo root in CI).",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html,list",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}

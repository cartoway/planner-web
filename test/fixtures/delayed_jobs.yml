# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

job_destination_geocoding:
  priority: 0
  attempts: 0
  handler: "A:A B:B"
  progress: { progress: 0 }

job_optimizer:
  priority: 0
  attempts: 0
  # Dummy planning_id so Job.on_planning is false unless a test retargets the handler.
  handler: "planning_id: 0"
  progress: { progress: 0 }
